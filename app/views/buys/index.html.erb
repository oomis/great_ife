<p id="notice"><%= notice %></p>

<h1>Buys</h1>

<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>Body</th>
      <th>Price</th>
      <th>Image</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @buys.each do |buy| %>
      <tr>
        <td><%= buy.title %></td>
        <td><%= buy.body %></td>
        <td><%= buy.price %></td>
        <td><%= buy.image %></td>
        <td><%= link_to 'Show', buy %></td>
        <td><%= link_to 'Edit', edit_buy_path(buy) %></td>
        <td><%= link_to 'Destroy', buy, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Buy', new_buy_path %>


<div>
  <ul class="nav justify-content-end">
  
</ul>
</div>




<nav class="navbar navbar-inverse">
  <div class="container">
    <div class="navbar-header">
      <%= link_to 'Oau E-buys', root_path, class: 'navbar-brand' %>
    </div>
    
    <div id="navbar">
      <ul class="nav navbar-nav pull-right">
        <% if buy_signed_in? %>
          <li class="dropdown">
            <a class="dropdown-toggle" data-toggle="dropdown" href="#">
              <span class="caret"></span>
            </a>
            <ul class="dropdown-menu" role="menu">
              <li><%= link_to 'Upload new product', new_buy_path %></li>
              <li><%= link_to 'Profile', edit_buy_registration_path %></li>
              <li><%= link_to 'Log out', destroy_buy_session_path, method: :delete %></li>
            </ul>
          </li>
        <% else %>
          <li style = "color: white;">Do you have something you want to sell, join campus biggest e-mall, just sign-up and sell now</li>
          <li><%= link_to 'Log In', new_buy_session_path %></li>
          <li><%= link_to 'Sign Up', new_buy_registration_path %></li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>


<p id="notice"><%= notice %></p>

<h1>buys</h1>

<div class ="d-flex flex-warp" style = "display: flex; flex-flow: row wrap;">
<% @buys.each do |buy| %>
<div class="card " style="width: 38rem; margin-bottom: 20px; padding-bottom: 20px">
  <img class="card-img-top" style ="    width: 355px; margin: 12px;" src="<%= buy.image.url %>" alt="Card image cap">
  <div class="card-body">
    <h2 style = "margin-left: 11px;" class="card-title"><u><%= buy.title %></u></h2>
    <p style = "margin-left: 11px;" class="card-text"><%= link_to truncate(buy.body, length: 70) %></p>
    <div>
    <h4 style = "margin-left: 23px; float: left" class="card-title"># <%= buy.price %></h4>
    <button style = "margin-right: 23px; float: right" class="btn btn-primary" type="submit"><%= link_to 'Show', buy %></button>
    </div>
    <% if buy_signed_in? %>
        <h5 style = "margin-left: 11px;"><%= link_to 'Edit', edit_buy_path(buy) %></h5>
        <h5 style = "margin-left: 11px;"><%= link_to 'Destroy', buy, method: :delete, data: { confirm: 'Are you sure?' } %></h5>
          
        <% end %>
  </div>
</div>
<% end %>
</div>

<% if buy_signed_in? %>
  <%= link_to 'Upload New Product', new_buy_path %>
<% end %>